# Build ESP-IDF
build/
.vscode/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock

# Secretos y material criptográfico — NUNCA se commitea
*.pem
*.key
*.der
secrets.yaml
secrets.*.yaml
!secrets.yaml.template
.env
.env.*

# Documentación operativa personal (ADR-0010) — NUNCA se commitea
docs/private/

# Artefactos de flasheo/depuración que pueden contener volcados de memoria
*.bin
*.elf.map
coredump*

# Binarios de tests host
tests/unit/test_h2

# Sistema
.DS_Store
*.swp
